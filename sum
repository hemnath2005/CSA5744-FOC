echo "Enter the number of values:"
read n
sum=0
echo "Enter the numbers:"
for (( i=1; i<=n; i++ ))
do
  read num
  sum=$((sum + num))
done
average=$((echo "$sum / $n"))

echo "Sum: $sum"
echo "Average: $average
